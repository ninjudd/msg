# Next

Committed to, not started. One line each, same rules as [now](now.md). See
[README](README.md) for how these lists work.

- [Context around a search hit](all/search-context.md) — `-A`/`-B`/`-C`, because
  a match on its own is a line torn out of a conversation.
