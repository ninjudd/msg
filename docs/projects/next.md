# Next

Committed to, not started. One line each, same rules as [now](now.md). See
[README](README.md) for how these lists work.

- [A daemon, so the terminal stops holding Full Disk Access](all/daemon-and-permissions.md)
  — a launchd agent holds the grant, the CLI holds none.
- Gate sending behind a config key, off by default
  ([§7](all/daemon-and-permissions.md)) — can land before the daemon does.
