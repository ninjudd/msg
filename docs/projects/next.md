# Next

Committed to, not started. One line each, same rules as [now](now.md). See
[README](README.md) for how these lists work.

- [Put a reaction on the message it reacted to](all/tapbacks.md) — `[😂♥♥]` after
  the body, instead of rows that interleave or are hidden.
- A date header when the day changes, and only a time on each message — what
  Messages does, instead of repeating the date on every line.
