# Next

Committed to, not started. One line each, same rules as [now](now.md). See
[README](README.md) for how these lists work.

- Gate sending behind a config key, off by default
  ([§7](all/daemon-and-permissions.md)) — the daemon shipped without it, so
  sending still runs from the CLI and nothing gates it but `--dry-run`.
