# Next

Committed to, not started. One line each, same rules as [now](now.md). See
[README](README.md) for how these lists work.

- Only open the Full Disk Access pane when the daemon does not already hold the
  grant — `msg daemon install` opens it unconditionally, which is constant noise
  when reinstalling a build that needs nothing granted.
